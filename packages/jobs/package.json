{
  "name": "@x1-starter/jobs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo .trigger node_modules",
    "lint": "biome check .",
    "jobs": "npx trigger.dev@latest dev",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^3.0.12"
  },
  "devDependencies": {
    "@trigger.dev/build": "^3.0.12"
  }
}
